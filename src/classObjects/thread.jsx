export class Thread {
    constructor(uid, title, description, replies, author) {
      this.uid = uid;
      this.title = title;
      this.description = description;
      this.replies = replies;
      this.author = author;
    }
  }